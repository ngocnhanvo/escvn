export interface Coupons {
  coupon_id?: number,
  coupon_code?: string,
  amount?: number, 
  calculated_discount?: number,
  discount_type?: string,
  coupon?: string,
  saving?: string,
  saving_html?: string
}